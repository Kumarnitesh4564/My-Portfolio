import { motion } from 'framer-motion';
import { FiGithub, FiGlobe, FiBookOpen } from 'react-icons/fi';
export function Projects({ projects }: { projects: Array<any> }) {
  return <section id='projects' className='section'><div className='section__heading'><span>Builds</span><h2>Projects</h2></div><div className='project-grid'>{projects.map((project, index) => <motion.article className='glass project-card' key={project.name} initial={{ opacity: 0, y: 28 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ delay: index * 0.1 }}><div className='project-card__image'><div className='project-card__overlay'>{project.image}</div></div><div className='project-card__content'><div className='card__kicker'>{project.type}</div><h3>{project.name}</h3><div className='chip-list'>{project.tech.map((tech: string) => <span key={tech}>{tech}</span>)}</div><ul>{project.features.map((feature: string) => <li key={feature}>{feature}</li>)}</ul><div className='project-actions'><a href={project.github}><FiGithub /> Github</a><a href={project.live}><FiGlobe /> Live Demo</a><a href={project.caseStudy}><FiBookOpen /> Case Study</a></div></div></motion.article>)}</div></section>;
}